{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "dev": {
      "cache": false
    },
    "build": {
      "inputs": [
        "**/*.ts"
      ],
      "outputs": [
        "dist/**",
        "build/**"
      ]
    },
    "build:dev": {
      "inputs": [
        "**/*.ts"
      ],
      "outputs": [
        "dist/**",
        "build/**"
      ]
    }
  }
}