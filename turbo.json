{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "dev": {
      "cache": false
    },
    "lint": {
      "inputs": [
        "**/*.{js,ts}"
      ]
    },
    "build": {
      "inputs": [
        "**/*.ts"
      ],
      "outputs": [
        "dist/**",
        "build/**"
      ]
    }
  }
}