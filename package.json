{
  "name": "zephyrjs",
  "scripts": {
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "build": "turbo run build",
    "build:dev": "turbo run build"
  },
  "devDependencies": {
    "turbo": "^1.6.3",
    "prettier": "^2.7.1"
  }
}